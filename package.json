{
  "name": "personal-computer-use",
  "version": "0.1.0",
  "private": true,
  "description": "LLM-driven computer-use agent for Windows (see ARCHITECTURE.md)",
  "scripts": {
    "start": "npm start --prefix electron",
    "postinstall": "npm install --prefix electron"
  }
}
